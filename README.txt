(c) This is a twitter tool designed to track the keywords and return analytics around their context such as frequency tables.
The tool generates its output in CSV and its input a configuurable to keywords and number of tweets with more functionality in the pipeline

All it needs is a twitter account and key.