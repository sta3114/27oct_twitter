© This is a twitter tool designed to track the keywords and return analytics around context such as frequency tables.

The tool generates its output in CSV and its input is configurable to keywords and number of tweets.

All it needs is a twitter API which you can get from apps.twitter.com